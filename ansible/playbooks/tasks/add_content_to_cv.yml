- name: "Add content tp content view"
  katello_content_view:
    username: "{{ username }}"
    password: "{{ password }}"
    server_url: "{{ server_url }}"
    name: "{{ item.cv_name }}"
    organization: "{{ item.organization }}"
    repositories:
      - name: '{{ item.repo_name }}'
        product: '{{ item.product }}'